using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Obstacle : MonoBehaviour
{
    public float obstacleTime = 5f;
    public bool locked = false;
    // Start is called before the first frame update
    void Start()
    {
        
    }

   
}
